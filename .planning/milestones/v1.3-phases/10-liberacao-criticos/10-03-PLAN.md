---
phase: '10-liberacao-criticos'
plan: '03'
title: 'Críticos Thresholds + Comunicação Email + Log/Escalação'
type: build
wave: 1
depends_on: ['10-01']
files_modified:
  - 'src/features/criticos/components/ThresholdsAdmin.tsx'
  - 'src/features/criticos/components/ThresholdForm.tsx'
  - 'src/features/criticos/components/CriticosFlag.tsx'
  - 'src/features/criticos/components/ComunicacaoModal.tsx'
  - 'src/features/criticos/components/ComunicacaoVerbalForm.tsx'
  - 'src/features/criticos/components/ComunicacaoTimeline.tsx'
  - 'src/features/criticos/utils/criticoDetector.ts'
  - 'src/features/criticos/services/thresholdService.ts'
  - 'src/features/criticos/services/comunicacaoService.ts'
  - 'src/features/criticos/hooks/useThresholds.ts'
  - 'src/features/criticos/hooks/useComunicacoes.ts'
  - 'functions/src/liberacao/detectarCriticos.ts'
  - 'functions/src/liberacao/enviarComunicacaoEmail.ts'
  - 'functions/src/liberacao/registrarComunicacaoVerbal.ts'
  - 'functions/src/liberacao/escalarCritico.ts'
  - 'functions/src/liberacao/_email/emailTemplates.ts'
  - 'functions/src/index.ts'
autonomous: false
requirements: ['CRI-01', 'CRI-02', 'CRI-03', 'CRI-04', 'CRI-05']
duration_estimate: '10 working days'
deadline: '2026-06-27'
priority: P1
user_setup:
  - service: resend
    why: 'Email transacional'
    env_vars: ['RESEND_API_KEY']
    dashboard_config:
      - task: 'Configurar SPF + DKIM + DMARC para hmatologia2.web.app'
        location: 'DNS provider'
      - task: 'Verificar dominio em Resend (notificacoes@hmatologia2.web.app)'
        location: 'Resend dashboard'
      - task: 'Configurar webhook bounce/complaint'
        location: 'Resend dashboard → webhooks'
must_haves:
  truths:
    - 'ThresholdsAdmin permite CRUD de thresholds por analito (min, max, severidade, condicional idade/sexo)'
    - 'criticoDetector engine puro: detecta crítico em 1 resultado dado thresholds aplicáveis'
    - 'detectarCriticos trigger onCreate em laudos: roda thresholds + flag laudo + dispara enviarComunicacaoEmail'
    - 'enviarComunicacaoEmail callable: envia email via Resend com PDF anexo (PDF gerado em Plan 10-04 — placeholder por ora)'
    - 'registrarComunicacaoVerbal: UI registra timestamp + receptor + RT comunicador + observação; LogicalSignature server-side'
    - 'escalarCritico cron 5min: aletra supervisor se SLA extrapolado (alerta 30min, bloqueador 60min, escalação 90min)'
    - 'ComunicacaoTimeline mostra histórico imutável com canal, receptor, sucesso/falha'
    - 'Webhook Resend bounce/complaint flagga email problemático no perfil médico'
    - 'Multi-tenant isolado; soft-delete only em thresholds'
    - 'Audit log imutável de toda comunicação'
  artifacts:
    - path: 'src/features/criticos/utils/criticoDetector.ts'
      provides: 'Engine puro: detectCriticoEm(resultado, thresholds, paciente)'
      contains: 'detectCriticoEm|threshold|severidade|idade|sexo'
      min_lines: 140
    - path: 'src/features/criticos/components/ThresholdsAdmin.tsx'
      provides: 'CRUD de thresholds por analito; UI dark-first'
      contains: 'tabular-nums|aria-label|softDelete|por analito'
      min_lines: 280
    - path: 'src/features/criticos/components/ComunicacaoModal.tsx'
      provides: 'Modal multi-canal: email automático OR registro verbal manual'
      contains: 'email|verbal|receptor|RT|signature|LogicalSignature'
      min_lines: 220
    - path: 'src/features/criticos/components/ComunicacaoTimeline.tsx'
      provides: 'Histórico imutável com canal, receptor, sucesso, motivo bounce'
      contains: 'timeline|imutável|sucesso|bounce|chainHash'
      min_lines: 160
    - path: 'functions/src/liberacao/detectarCriticos.ts'
      provides: 'Trigger onCreate de laudos: detecta crítico + dispara comunicação'
      contains: 'onDocumentCreated|laudos|criticoDetector|enviarComunicacaoEmail'
      min_lines: 180
    - path: 'functions/src/liberacao/enviarComunicacaoEmail.ts'
      provides: 'Callable: envia email via Resend; tracking opcional; webhook bounce/complaint'
      contains: 'Resend|nodemailer|attachment|webhook|bounce'
      min_lines: 220
    - path: 'functions/src/liberacao/escalarCritico.ts'
      provides: 'Cron 5min: alerta supervisor se SLA extrapolado'
      contains: 'onSchedule|SLA|extrapolado|supervisor'
      min_lines: 180
  key_links:
    - from: 'Laudo criado em /laudos'
      to: 'detectarCriticos trigger'
      via: 'Firestore onDocumentCreated'
      pattern: 'onDocumentCreated.*laudos'
    - from: 'detectarCriticos'
      to: 'enviarComunicacaoEmail'
      via: 'Internal call (Admin SDK)'
      pattern: 'enviarComunicacaoEmail'
    - from: 'ComunicacaoModal verbal'
      to: 'registrarComunicacaoVerbal callable'
      via: 'httpsCallable + signature'
      pattern: 'registrarComunicacaoVerbal'
    - from: 'Resend webhook'
      to: 'Função flagBounce'
      via: 'HTTPS webhook'
      pattern: 'webhook.*bounce'
---

<objective>
Sistema completo de valores críticos: cadastro de thresholds parametrizáveis (por analito + condicional idade/sexo), detecção automática server-side, comunicação por email (Resend) com PDF anexo, fallback de comunicação verbal com registro auditável, escalação automática se SLA não cumprido.

**Output:** RT cadastra thresholds → laudo criado dispara detecção → email enviado → se não comunicado em 60min, escala supervisor. Toda comunicação tem audit log imutável.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/references/ui-brand.md

**Phase context:** @.planning/phases/10-liberacao-criticos/CONTEXT.md
**Depends on:** Plan 10-01 (types + service)
**Parallel with:** Plan 10-02 (3+ days overlap após types prontos)

**Module pattern reference (READ FIRST):**

- `src/features/auditoria/components/` — pattern de audit log timeline
- `functions/src/_email/` (se existir) — Resend setup
- `src/features/educacao-continuada/` — assinatura digital pattern

**Skill:** `hcq-firestore-rules-generator` (validar bloco rules)

</execution_context>

<tasks>

### Task 1: criticoDetector engine (1 dia)

- `src/features/criticos/utils/criticoDetector.ts`:

  ```typescript
  export interface CriticoThreshold {
    id: string;
    analitoId: string;
    analitoNome: string;
    min: number | null;
    max: number | null;
    unidade: string;
    severidade: 'alta' | 'baixa';
    condicional?: { idadeMin?: number; idadeMax?: number; sexo?: 'M' | 'F' };
    ativo: boolean;
  }

  export function detectCriticoEm(
    resultado: { value: number; analitoId: string; unidade: string },
    thresholds: CriticoThreshold[],
    paciente: { idade: number; sexo: 'M' | 'F' | 'NI' },
  ): {
    isCritico: boolean;
    threshold?: CriticoThreshold;
    severidade?: 'alta' | 'baixa';
    reason?: string;
  } {
    const applicable = thresholds.filter((t) => {
      if (!t.ativo) return false;
      if (t.analitoId !== resultado.analitoId) return false;
      if (t.unidade !== resultado.unidade) return false;
      if (t.condicional?.sexo && t.condicional.sexo !== paciente.sexo) return false;
      if (t.condicional?.idadeMin && paciente.idade < t.condicional.idadeMin) return false;
      if (t.condicional?.idadeMax && paciente.idade > t.condicional.idadeMax) return false;
      return true;
    });

    for (const t of applicable) {
      if (t.min !== null && resultado.value < t.min)
        return {
          isCritico: true,
          threshold: t,
          severidade: t.severidade,
          reason: `Abaixo de ${t.min}`,
        };
      if (t.max !== null && resultado.value > t.max)
        return {
          isCritico: true,
          threshold: t,
          severidade: t.severidade,
          reason: `Acima de ${t.max}`,
        };
    }
    return { isCritico: false };
  }

  export function detectAllCriticos(
    laudo: Laudo,
    thresholds: CriticoThreshold[],
  ): {
    hasCritico: boolean;
    criticos: { exameId: string; threshold: CriticoThreshold; reason: string }[];
  } {
    const criticos: any[] = [];
    for (const exame of laudo.exames) {
      for (const r of exame.resultados) {
        const res = detectCriticoEm(
          { value: r.value, analitoId: exame.id, unidade: r.unidade },
          thresholds,
          { idade: laudo.pacienteIdade.value, sexo: laudo.paciente.sexo },
        );
        if (res.isCritico)
          criticos.push({ exameId: exame.id, threshold: res.threshold!, reason: res.reason! });
      }
    }
    return { hasCritico: criticos.length > 0, criticos };
  }
  ```

- Tests: 20+ cenários cobrindo condicionais idade/sexo, múltiplos thresholds aplicáveis, edge cases (unidade não-match)

**Acceptance:** Coverage ≥98%; engine puro.

### Task 2: ThresholdsAdmin UI (1.5 dia)

- `src/features/criticos/components/ThresholdsAdmin.tsx`:
  - Tabela: analito | min | max | unidade | severidade | condicional (idade/sexo) | ativo | ações
  - Filtro: por analito; ativo/inativo
  - CRUD via modal `ThresholdForm.tsx`
  - Soft-delete only
  - Multi-tenant scoped
  - Dark-first; accent vermelho para "alta", amarelo para "baixa"
- `ThresholdForm.tsx`:
  - Select analito (lista de analitos do lab — leitura cross-module: bioquimica, hematologia, etc)
  - Inputs min/max (numérico, tabular-nums)
  - Select unidade (popula baseado no analito)
  - Toggle condicional idade/sexo; expande inputs se ativo
  - Severidade: radio (alta/baixa)
  - Validação Zod inline; submit via service

**Acceptance:** A11y AA; foco visível; navegação teclado completa.

### Task 3: Service layer (1 dia)

- `src/features/criticos/services/thresholdService.ts`:
  - `subscribeThresholds(labId, callback): Unsubscribe`
  - `createThreshold(labId, input): Promise<string>` (deprecated: callable em v1.4)
  - `updateThreshold(labId, id, patch): Promise<void>`
  - `softDeleteThreshold(labId, id, operatorId): Promise<void>`
- `src/features/criticos/services/comunicacaoService.ts`:
  - `subscribeComunicacoes(labId, laudoId, callback): Unsubscribe`
  - `enviarEmail(labId, laudoId): Promise<void>` (chama callable)
  - `registrarVerbal(labId, laudoId, payload): Promise<void>` (chama callable)

**Acceptance:** Listeners cleanup; multi-tenant.

### Task 4: detectarCriticos trigger (1.5 dia)

- `functions/src/liberacao/detectarCriticos.ts`:
  - Firestore trigger `onDocumentCreated('labs/{labId}/laudos/{laudoId}')`
  - Pipeline:
    1. Lê laudo criado
    2. Lê thresholds ativos do lab
    3. Roda `detectAllCriticos(laudo, thresholds)`
    4. Se hasCritico:
       - Update laudo: `criticoFlag: true`, `criticoDetalhes: criticos`
       - Chama `enviarComunicacaoEmail` (interno)
       - Cria audit log entry
    5. Se !hasCritico: skip silenciosamente
- Idempotência: usa `eventId` do trigger como doc id

**Acceptance:** Trigger executa em <2s; 100 laudos batch criados → 100 detecções corretas.

### Task 5: enviarComunicacaoEmail callable (2 dias)

- `functions/src/liberacao/enviarComunicacaoEmail.ts`:
  - Auth: `isActiveMemberOfLab` (callable manual) ou internal trigger
  - Input: `{ labId, laudoId, isAutomatic: bool }`
  - Pipeline:
    1. Lê laudo + médico solicitante
    2. Gera/lê PDF (placeholder por ora — Plan 10-04 implementa generateLaudoPDF)
    3. Carrega template email apropriado (`emailTemplates.ts`):
       - Template "laudo-critico-alta" (header vermelho)
       - Template "laudo-critico-baixa" (header amarelo)
       - Template "laudo-rotina" (header neutro — usado em Plan 10-04 para laudos sem crítico)
    4. Envia via Resend SDK:
       ```typescript
       resend.emails.send({
         from: 'notificacoes@hmatologia2.web.app',
         to: medicoSolicitante.email,
         subject: subjectTemplate(...),
         html: bodyTemplate(...),
         attachments: [{ filename: `laudo-{paciente_iniciais}.pdf`, content: pdfBuffer }],
         tags: [{ name: 'tipo', value: 'critico' }, { name: 'labId', value: labId }],
       })
       ```
    5. Cria doc Comunicacao com `canal: 'email'`, `sucesso: true` (atualizado por webhook)
    6. Retorna `{ comunicacaoId, status: 'sent' }`
  - Region: `southamerica-east1`, memory: `512MiB`, timeout: `60s`
  - Secret: `RESEND_API_KEY`
- Webhook handler `functions/src/liberacao/resendWebhook.ts`:
  - HTTPS endpoint público (verificado via Resend signature)
  - Eventos: `email.delivered`, `email.bounced`, `email.complained`
  - Update doc Comunicacao + flagga email problemático em `medicos-solicitantes`

**Acceptance:** Email entrega ≥98% inbox em teste com 10 destinos diferentes; bounce flagga corretamente.

### Task 6: registrarComunicacaoVerbal callable (1 dia)

- `functions/src/liberacao/registrarComunicacaoVerbal.ts`:
  - Auth: `isActiveMemberOfLab` + claim RT
  - Input Zod: `{ labId, laudoId, receptorTipo, receptorNome, receptorContato?, observacao?, signaturePayload }`
  - Pipeline:
    1. Valida laudo existe + criticoFlag
    2. Recalcula chainHash server-side
    3. Cria doc Comunicacao com `canal: 'verbal'`, `sucesso: true`, signature server-side
    4. Audit log
  - Idempotência: hash do payload

**Acceptance:** Comunicação verbal cria doc imutável; audit log presente.

### Task 7: ComunicacaoModal + ComunicacaoVerbalForm + ComunicacaoTimeline (2 dias)

- `src/features/criticos/components/ComunicacaoModal.tsx`:
  - Tabs: **Email** | **Verbal**
  - Aba Email: preview do email a ser enviado (subject + body) + destino + botão "Enviar agora"
  - Aba Verbal: form (receptor tipo, nome, contato, observação) + RTSignatureGate
- `ComunicacaoVerbalForm.tsx`: form components reusable
- `ComunicacaoTimeline.tsx`:
  - Lista cronológica de Comunicacao docs (mais recente no topo)
  - Cada item: ícone canal (email/verbal), receptor, RT comunicador, status (sucesso/bounce/complained), timestamp
  - Hover: tooltip com chainHash truncado + observação completa

**Acceptance:** Timeline carrega <1s; sem race conditions em update real-time.

### Task 8: escalarCritico cron (1 dia)

- `functions/src/liberacao/escalarCritico.ts`:
  - `onSchedule({ schedule: 'every 5 minutes', region: 'southamerica-east1' }, ...)`
  - Pipeline:
    1. Query labs com laudos `criticoFlag: true && status != 'Comunicado'`
    2. Para cada laudo:
       - Calcular tempo desde `criadoEm`
       - Se >30min: alerta visual no dashboard (set flag `criticoAlerta: true`)
       - Se >60min: bloquear status `Liberado` (set flag `criticoBloqueador: true`)
       - Se >90min: enviar email a supervisor (claim `supervisor`) e tentar SMS no v1.4
    3. Audit log por escalação
  - Idempotência: track `ultimaEscalacao` por laudo, não duplica alertas

**Acceptance:** Cron executa pontualmente; SLAs respeitados em 50 laudos teste.

### Task 9: Email templates HTML (0.5 dia)

- `functions/src/liberacao/_email/emailTemplates.ts`:
  - `subjectTemplate({ tipo, paciente_iniciais, labName })` — string template
  - `bodyTemplate({ tipo, paciente, exames, criticos, laudoUrl })` — HTML template
  - 3 templates: critico-alta (vermelho), critico-baixa (amarelo), rotina (neutro)
  - Brand neutro: logo HC Quality discreto; cores semânticas; CTA primário "Ver laudo"
  - Plain text fallback (para clientes sem HTML)

**Acceptance:** Templates renderizam corretamente em Gmail, Outlook, Apple Mail; sem warnings de spam (CAN-SPAM compliant).

### Task 10: Hooks (0.5 dia)

- `useThresholds(labId)` — onSnapshot
- `useComunicacoes(labId, laudoId)` — onSnapshot por laudo
- `useCriticosFlag(laudoId)` — derivado; retorna `{ hasCritico, severidade, count }`

**Acceptance:** Sem warnings; cleanup ok.

</tasks>

<deviation_protocol>
Se Resend webhook não chegar em 24h após teste: investigar configuração DNS (SPF/DKIM/DMARC) ou domínio não verificado.
Se thresholds condicionais (idade/sexo) gerarem complexidade UI: começar simples (apenas analito + min/max + severidade), adicionar condicionais em iteração separada.
Se cron escalarCritico atrasar (>10min): investigar quota Cloud Functions; pode precisar memory boost.
Se email entregar em spam mesmo com SPF/DKIM/DMARC: warm-up domain (enviar 10/dia primeira semana, escalar gradual).
</deviation_protocol>

<post_plan_gates>

1. `tsc --noEmit` 0 erros
2. Coverage: criticoDetector ≥98%; callables ≥85%
3. Integration test: laudo crítico criado → trigger detecta → email enviado → webhook recebido
4. Smoke local: 10 emails diferentes — ≥98% inbox
5. Cron escalarCritico executa em emulador
6. Deploy: rules + functions:detectarCriticos,enviarComunicacaoEmail,registrarComunicacaoVerbal,escalarCritico + hosting
7. Atualizar STATE.md `10-03: COMPLETE`
8. Commit: `feat(10-liberacao-criticos): críticos thresholds + email + escalação SLA`
   </post_plan_gates>
