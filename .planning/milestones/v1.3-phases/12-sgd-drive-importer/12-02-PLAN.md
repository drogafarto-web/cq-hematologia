---
phase: '12-sgd-drive-importer'
plan: '02'
title: 'UI — LM-01 Dashboard + Hierarquia Tree + Distribuição Matrix'
type: build
wave: 1
depends_on: ['12-01']
files_modified:
  - 'src/features/sgq/components/lm/ListaMestraDashboard.tsx'
  - 'src/features/sgq/components/lm/ListaMestraTable.tsx'
  - 'src/features/sgq/components/lm/TipoDocumentoBadge.tsx'
  - 'src/features/sgq/components/lm/StatusVigenciaBadge.tsx'
  - 'src/features/sgq/components/lm/ListaMestraFilters.tsx'
  - 'src/features/sgq/components/hierarquia/HierarquiaTree.tsx'
  - 'src/features/sgq/components/hierarquia/HierarquiaNode.tsx'
  - 'src/features/sgq/components/hierarquia/HierarquiaPath.tsx'
  - 'src/features/sgq/components/distribuicao/DistribuicaoMatrix.tsx'
  - 'src/features/sgq/components/distribuicao/SetorColumn.tsx'
  - 'src/features/sgq/components/distribuicao/MeusDocsAlert.tsx'
  - 'src/features/sgq/components/transitions/TransicaoVigenciaModal.tsx'
  - 'src/AppRouter.tsx'
  - 'src/features/hub/HubTiles.tsx'
  - 'functions/src/sgq/transitarVigencia.ts'
  - 'functions/src/index.ts'
autonomous: false
requirements: ['SGD-03', 'SGD-04', 'SGD-05', 'SGD-14']
duration_estimate: '7 working days'
deadline: '2026-07-08'
priority: P1
must_haves:
  truths:
    - 'ListaMestraDashboard: lista paginada 50/page; filtros (tipo, status, setor, período); ordenação'
    - 'TipoDocumentoBadge: 15 tipos com cores semânticas (MQ amarelo, PQ azul, IT verde, FR laranja, POL roxo, etc.)'
    - 'StatusVigenciaBadge: 4 estados (draft, em-revisao, vigente, obsoleto) com cores'
    - 'HierarquiaTree: visualização vertical MQ→PQ→IT→FR; colapsar/expandir; navegável teclado'
    - 'HierarquiaNode: card por doc com tipo, código, título, status; click navega'
    - 'DistribuicaoMatrix: matriz 15 tipos × 17 setores; cell highlight quando doc distribuído; filtros'
    - "MeusDocsAlert: banner para usuário 'Você tem N docs novos no seu setor'"
    - "TransicaoVigenciaModal: RT clica 'Aprovar' → modal confirmação + signature → transitarVigencia callable"
    - 'transitarVigencia callable: valida transição (state machine) + signature + chainHash + audit'
    - 'Lazy loading + virtualização (>50 rows)'
    - 'A11y AA; tabular-nums em colunas numéricas; foco visível'
  artifacts:
    - path: 'src/features/sgq/components/lm/ListaMestraDashboard.tsx'
      provides: 'Dashboard catálogo unificado'
      contains: 'filtros|paginação|tipo|status|setor|tabular-nums'
      min_lines: 280
    - path: 'src/features/sgq/components/hierarquia/HierarquiaTree.tsx'
      provides: 'Árvore vertical interativa'
      contains: 'colapsar|expandir|MQ|PQ|IT|FR|aria-tree'
      min_lines: 240
    - path: 'src/features/sgq/components/distribuicao/DistribuicaoMatrix.tsx'
      provides: 'Matriz docs × setores; virtualizada se >50 rows'
      contains: 'matrix|setores|tipos|virtualizada|MeusDocsAlert'
      min_lines: 280
    - path: 'src/features/sgq/components/transitions/TransicaoVigenciaModal.tsx'
      provides: 'Modal aprovação RT + signature'
      contains: 'TransicaoVigencia|RTSignature|draft|em-revisao|vigente'
      min_lines: 200
    - path: 'functions/src/sgq/transitarVigencia.ts'
      provides: 'Callable RT: muda statusVigencia + signature + chainHash'
      contains: 'isActiveMemberOfLab|claim.*RT|validateTransition|chainHash'
      min_lines: 220
  key_links:
    - from: 'LisaMestraDashboard'
      to: 'useHierarquia + useDistribuicao hooks'
      via: 'Real-time data'
      pattern: 'useHierarquia|useDistribuicao'
    - from: 'HierarquiaNode click'
      to: 'ListaMestraDashboard com doc selecionado'
      via: 'URL deep link'
      pattern: "/sgq/lista-mestra\\?docId="
    - from: 'MeusDocsAlert'
      to: 'useDocsForUser hook'
      via: 'Sync com /personnel/cargos'
      pattern: 'useDocsForUser'
---

<objective>
3 surfaces visuais + 1 modal de transição: catálogo unificado LM-01, árvore hierárquica navegável, matriz de distribuição docs × setores.

**Output:** Qualidade abre `/sgq/lista-mestra` → vê todos os docs do lab; abre `/sgq/hierarquia` → vê tree MQ→PQ→IT→FR; abre `/sgq/distribuicao` → vê matriz docs × setores; aprova doc draft com signature.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/references/ui-brand.md

**Phase context:** @.planning/phases/12-sgd-drive-importer/CONTEXT.md
**Depends on:** Plan 12-01

**Module pattern reference:**

- `src/features/auditoria/components/AchadoTimeline.tsx` (timeline pattern)
- `src/features/liberacao/components/StatusBadge.tsx` (Plan 10-02 — replicar pattern)
- `src/features/pops/components/POPApproval*.tsx` (RT signature pattern)

</execution_context>

<tasks>

### Task 1: TipoDocumentoBadge + StatusVigenciaBadge (0.5 dia)

- 15 tipos com cores semânticas dark-first
- 4 estados statusVigencia
- Tooltips explicativos

**Acceptance:** A11y AA; tooltips funcionam.

### Task 2: ListaMestraTable + Filters + Dashboard (2 dias)

- Tabela com colunas: código (tabular-nums), tipo (badge), título, status, versão, setores LD (chips), última atualização, ações
- Filtros: tipo (dropdown 15), status (4 chips), setor (dropdown 17), período
- URL params persistence (deep linkable)
- Paginação 50/page; total count visível
- Empty state, loading skeleton
- Hover row → highlight; click row → detail

**Acceptance:** LCP <2.5s com 100 docs; INP <200ms ao filtrar.

### Task 3: HierarquiaTree + Node + Path (2 dias)

- Árvore vertical: MQ-001 raiz → PQs → ITs → FRs
- Colapsar/expandir por nível (default: MQ + PQs expandidos)
- Click node → navega para detail
- Path breadcrumbs no topo (se navegado por click)
- Roles ARIA: `role="tree"`, `role="treeitem"`, `aria-expanded`
- Navegação teclado: arrow keys

**Acceptance:** A11y AA; renderiza 80 nodes <500ms.

### Task 4: DistribuicaoMatrix + SetorColumn (2 dias)

- Layout: linhas = docs, colunas = 17 setores
- Cell ✓ se doc distribuído ao setor; vazio caso contrário
- Hover cell: tooltip "PQ-15 distribuído para Bioquímica"
- Filtros: tipo doc, setor (foco em uma coluna)
- Virtualização vertical se >50 docs (react-window)
- Export CSV button (admin only)

**Acceptance:** Renderiza 80 docs × 17 setores <1s; virtual scroll funciona.

### Task 5: MeusDocsAlert (0.5 dia)

- Banner top dashboard: "Você tem N docs novos no seu setor"
- Lê via useDocsForUser; conta docs `vigente` com `setor in user.cargos`
- Click → drawer com lista; mark as read
- Persiste `lastSeenLDAt` em user prefs

**Acceptance:** Banner aparece quando aplicável; some ao mark read.

### Task 6: TransicaoVigenciaModal (1 dia)

- Trigger: button "Aprovar" em doc draft
- Modal: preview do doc + reason input + RTSignatureGate (PIN)
- Submit: chama transitarVigencia callable

**Acceptance:** UX clara; RT aprova em <30s.

### Task 7: transitarVigencia callable (1 dia)

- Auth: claim RT
- State machine: draft → em-revisao | em-revisao → vigente | vigente → obsoleto
- Pipeline: validar transição + signature + chainHash + audit log

**Acceptance:** Race condition test; valid transitions only.

### Task 8: Routing + Hub tiles (0.25 dia)

- `/sgq/lista-mestra`, `/sgq/hierarquia`, `/sgq/distribuicao`
- Hub tile sgq atualizado com sub-tiles para 3 surfaces

**Acceptance:** Build succeeds.

</tasks>

<post_plan_gates>

1. `tsc --noEmit` 0 erros
2. 3 surfaces carregam smoke local com 30 docs seed
3. Web Vitals: LCP <2.5s (Hierarquia <3s aceito); CLS <0.1
4. A11y AA: 0 violations axe-core
5. Deploy: functions:transitarVigencia + hosting
6. STATE.md `12-02: COMPLETE`
7. Commit: `feat(12-sgd): UI lista-mestra + hierarquia tree + distribuição matrix + transição RT`
   </post_plan_gates>
