---
phase: "12-sgd-drive-importer"
plan: "01"
title: "Schema Extension SGQ + Multi-Tenant + Hierarquia"
type: build
wave: 1
depends_on: []
files_modified:
  - "src/features/sgq/types/documento.ts"
  - "src/features/sgq/types/hierarquia.ts"
  - "src/features/sgq/types/listaDistribuicao.ts"
  - "src/features/sgq/types/driveImport.ts"
  - "src/features/sgq/types/setor.ts"
  - "src/features/sgq/services/documentoService.ts"
  - "src/features/sgq/services/hierarquiaService.ts"
  - "src/features/sgq/services/distribuicaoService.ts"
  - "src/features/sgq/utils/hierarquiaResolver.ts"
  - "src/features/sgq/utils/setorNormalizer.ts"
  - "src/features/sgq/constants/tiposDocumento.ts"
  - "src/features/sgq/constants/setoresRiopomba.ts"
  - "src/features/sgq/hooks/useHierarquia.ts"
  - "src/features/sgq/hooks/useDistribuicao.ts"
  - "src/features/sgq/CLAUDE.md"
  - "firestore.rules"
  - "firestore.indexes.json"
  - "functions/src/sgq/sincronizarLDPessoal.ts"
  - "functions/src/sgq/gerarHierarquiaCache.ts"
autonomous: false
requirements: ["SGD-01", "SGD-02", "SGD-13"]
duration_estimate: "7 working days"
deadline: "2026-06-27"
priority: P1
must_haves:
  truths:
    - "Documento type estendido: enum tipos 5→15, campos listaDistribuicao + hierarquia + urlDriveOriginal + importedFromDrive + statusVigencia"
    - "Backward compatible: docs SGQ existentes ainda funcionam (campos novos opcionais com defaults)"
    - "hierarquiaResolver engine puro: dado um doc, retorna parent + derivados; cache com invalidação"
    - "setorNormalizer engine puro: 'Bioquímica' → 'bioquimica' (ASCII slug, ignore acentos)"
    - "Constants: 15 tipos documento + 17 setores Riopomba seed"
    - "Trigger sincronizarLDPessoal: onUpdate em /personnel/cargos → propaga LD changes"
    - "gerarHierarquiaCache: rebuilds tree quando docs mudam (debounce 5s)"
    - "Multi-tenant: labId enforcement em todas as queries; cross-tenant blocked por rules"
    - "Firestore rules: validSignature + labIdMatches; statusVigencia transitions validadas"
    - "TypeScript strict: 0 erros"
  artifacts:
    - path: "src/features/sgq/types/documento.ts"
      provides: "Documento estendido com campos Phase 12"
      contains: "listaDistribuicao|hierarquia|urlDriveOriginal|importedFromDrive|statusVigencia|aprovadoPorRtId"
      min_lines: 180
    - path: "src/features/sgq/utils/hierarquiaResolver.ts"
      provides: "Engine puro: resolve parent + derivados; cache invalidation"
      contains: "resolveHierarquia|parent|derivados|cache"
      min_lines: 120
    - path: "src/features/sgq/utils/setorNormalizer.ts"
      provides: "Engine puro: nome → slug ASCII"
      contains: "normalizeSetor|slug|ASCII|acentos"
      min_lines: 60
    - path: "src/features/sgq/constants/tiposDocumento.ts"
      provides: "15 tipos: MQ, PQ, IT, FR, POL, DC, IC, ITA, ITE, CCE, FISPQ, RDC, ME, OD, TB"
      contains: "MQ|PQ|IT|FR|POL|DC|IC|ITA|ITE|CCE|FISPQ|RDC|ME|OD|TB"
      min_lines: 80
    - path: "src/features/sgq/constants/setoresRiopomba.ts"
      provides: "17 setores Riopomba (9 analíticos + 8 administrativos)"
      contains: "bioquimica|hematologia|coagulacao|recepcao|17"
      min_lines: 60
    - path: "functions/src/sgq/sincronizarLDPessoal.ts"
      provides: "Trigger onUpdate /personnel/cargos: propaga LD changes"
      contains: "onDocumentUpdated|cargos|listaDistribuicao|propagar"
      min_lines: 160
  key_links:
    - from: "Personnel cargo update"
      to: "sincronizarLDPessoal trigger"
      via: "Firestore onUpdate"
      pattern: "onDocumentUpdated.*cargos"
    - from: "Doc statusVigencia change"
      to: "gerarHierarquiaCache trigger"
      via: "Cache invalidation"
      pattern: "gerarHierarquiaCache"
---

<objective>
Estender schema SGQ existente com campos Phase 12 (LD, hierarquia, urlDriveOriginal, statusVigencia), engines puros (hierarquiaResolver, setorNormalizer), constants (15 tipos + 17 setores), triggers (sync LD via /personnel + cache hierarquia).

**Output:** SGQ pronto para receber UI Phase 12 (Plan 12-02) e Drive importer (Plan 12-03). Backward-compatible com docs SGQ existentes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md

**Phase context:** @.planning/phases/12-sgd-drive-importer/CONTEXT.md
**Module pattern reference:**
- `src/features/sgq/` — base existente (estender, não substituir)
- `src/features/personnel/` (Phase 8) — leitura para LD dinâmica
- `src/features/auditoria/` — chainHash + audit pattern

**Skill:** `hcq-firestore-rules-generator`
</execution_context>

<tasks>

### Task 1: Tipos estendidos (1.5 dia)
- `documento.ts` extends `DocumentoSGQ` (já existe) com 5 campos novos
- `hierarquia.ts`, `listaDistribuicao.ts`, `driveImport.ts`, `setor.ts`
- Backward compat: campos novos opcionais com defaults

**Acceptance:** `tsc --noEmit` 0 erros; SGQ existente continua funcionando.

### Task 2: hierarquiaResolver engine (1 dia)
- `resolveHierarquia(documentoId, allDocs): { parent, derivados[], path[] }`
- Cache LRU 1h
- Invalidação on doc change

**Acceptance:** Coverage ≥98%; performance <50ms para 80 docs.

### Task 3: setorNormalizer engine (0.5 dia)
- ASCII slug: 'Bioquímica' → 'bioquimica', 'RH/Adm' → 'rh-adm'
- Reversível: keep map normalized → display name

**Acceptance:** 17 setores Riopomba + edge cases (espaços, acentos, /, \) testados.

### Task 4: Constants (0.5 dia)
- `tiposDocumento.ts`: 15 tipos com label pt-BR + descrição + grupo (MQ, PQ, IT, etc.)
- `setoresRiopomba.ts`: 17 setores seed (9 analíticos + 8 administrativos) com labels

**Acceptance:** Constantes validadas com inventário Drive (Obsidian).

### Task 5: Service layer (1 dia)
- `documentoService.ts` (estender existente): subscribe filtros novos (statusVigencia, tipo expandido, listaDistribuicao)
- `hierarquiaService.ts`: read cache; rebuild on demand
- `distribuicaoService.ts`: query `listaDistribuicao` + sync via /personnel

**Acceptance:** Listener cleanup; sem regressão em sgq.

### Task 6: Hooks (0.5 dia)
- `useHierarquia(labId, documentoId)`: tree resolved
- `useDistribuicao(labId, setorId)`: docs distribuídos para o setor
- `useDocsForUser(uid)`: docs do setor do usuário (cross-module com Phase 8)

**Acceptance:** Performance + memoização ok.

### Task 7: Triggers Cloud Functions (1.5 dia)
- `sincronizarLDPessoal`: onUpdate `/personnel/cargos`
  - Lê before vs after; se setor mudou, atualiza notificações em users
  - Audit log
- `gerarHierarquiaCache`: onWrite `/sgq/documentos/*`
  - Debounce 5s; rebuild tree cache
  - Persist em `/sgq/hierarquia-cache/{labId}`

**Acceptance:** Triggers idempotentes; race conditions handled.

### Task 8: Firestore rules + indexes (1 dia)
- Rules para campos novos (listaDistribuicao, hierarquia, statusVigencia)
- Validação transição statusVigencia (draft → em-revisao → vigente → obsoleto)
- Indexes: `(labId, tipo, statusVigencia, criadoEm DESC)`, `(labId, listaDistribuicao, statusVigencia)`

**Acceptance:** Emulator tests verde (15+ casos).

### Task 9: CLAUDE.md SGQ (0.5 dia)
- Atualizar `src/features/sgq/CLAUDE.md` com decisões Phase 12

**Acceptance:** Diff claro; ≤200 linhas total.

</tasks>

<post_plan_gates>
1. `tsc --noEmit` 0 erros (web + functions)
2. SGQ existente em prod não regredido (smoke /sgq atual ok)
3. Coverage hierarquiaResolver + setorNormalizer ≥98%
4. Triggers funcionam em emulator
5. Rule tests verde
6. Deploy: rules + indexes + functions:sincronizarLDPessoal,gerarHierarquiaCache
7. STATE.md `12-01: COMPLETE`
8. Commit: `feat(12-sgd): schema extension + hierarquia + LD dinâmica + multi-tenant`
</post_plan_gates>
