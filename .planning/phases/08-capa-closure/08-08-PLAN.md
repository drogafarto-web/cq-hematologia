---
phase: 08-capa-closure
plan: '08'
wave: 7
label: Verification Gate
model: haiku
type: execute
depends_on: ['08-07']
parallel: false
autonomous: true
human_gates: 0
subagents: 1
---

# Phase 8 — Wave 7: Verification Gate (1 subagente)

deps: W6 completo. Gate final — bloqueia se qualquer check falhar.

---

## SA-45 — Gate Final

Verificação automatizada de toda a fase.

**Checks (em ordem, parar no primeiro erro):**

```bash
# 1. TypeScript — zero erros
npx tsc --noEmit 2>&1 | grep "error TS" && exit 1 || echo "✓ TSC clean"

# 2. Build app — zero erros
npm run build 2>&1 | grep -E "^(error|Error)" && exit 1 || echo "✓ Build clean"

# 3. Functions build — zero erros
cd functions && npm run build 2>&1 | grep "error TS" && exit 1 || echo "✓ Functions TSC clean"
cd ..

# 4. Tests — todos passando
npm test -- --passWithNoTests 2>&1 | grep "FAIL" && exit 1 || echo "✓ Tests pass"

# 5. Firestore rules — bloco capa-tracking presente
grep -q "capa-tracking" firestore.rules && echo "✓ Rules updated" || exit 1

# 6. Functions index — novos callables exportados
grep -q "createCapa" functions/src/index.ts && echo "✓ Callables wired" || exit 1

# 7. Hub tile — capa-tracking registrado
grep -q "capa-tracking" src/features/hub/ModuleHub.tsx && echo "✓ Hub tile present" || exit 1
```

**Se todos os checks passam:** criar `.planning/phases/08-capa-closure/08-VERIFICATION.md` com:

```markdown
# Phase 8 — Verification Report

Date: {ISO timestamp}
Status: ✅ ALL CHECKS PASSED

- TSC: 0 errors
- Build: clean
- Functions: 0 errors
- Tests: all passing
- Rules: capa-tracking block present
- Functions index: 7 callables wired
- Hub: tile registered
```

**Se qualquer check falha:** listar erros encontrados, NÃO criar o arquivo de verificação.

**Verify (do gate):** `test -f .planning/phases/08-capa-closure/08-VERIFICATION.md && echo "Phase 8 complete"`
