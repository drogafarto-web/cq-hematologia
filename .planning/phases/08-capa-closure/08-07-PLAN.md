---
phase: 08-capa-closure
plan: "07"
wave: 6
label: Tests
model: haiku
type: execute
depends_on: ["08-06"]
parallel: true
autonomous: true
human_gates: 0
subagents: 4
---

# Phase 8 — Wave 6: Tests (4 subagentes paralelos)

deps: W5 completo. Todos os 4 SAs disparam simultaneamente.

---

## SA-41 — `src/__tests__/capa-tracking/capa-state-machine.test.ts`

Testes unitários para state machine CAPA. ~80 LOC.

**Testa:**
- `isValidStateTransition`: todas as 20+ combinações (valid + invalid)
- `daysRemaining`: deadline futuro, passado, hoje
- `CapaDocument` schema: campos obrigatórios presentes
- Evidência com hash inválido (<64 chars): rejeitar
- Transição `closed → qualquer`: inválida

**Framework:** Vitest (se disponível) ou Jest  
**Verify:** `npm test -- --testPathPattern="capa-state-machine" 2>&1 | grep -E "(PASS|FAIL)"`

---

## SA-42 — `src/__tests__/capa-tracking/capa-service.test.ts`

Testes unitários para capaService com Firestore mockado. ~100 LOC.

**Testa (mock `firebase/firestore`):**
- `subscribeToCapas`: chama `onSnapshot`, cleanup retorna unsubscribe
- `listCapas` com filterState: query inclui `where('state', ...)`
- `getCapaById`: retorna null se doc não existe
- `daysRemaining` calculado em cada read (não cached)

**Mock:** `vi.mock('firebase/firestore')` com implementações mínimas  
**Verify:** `npm test -- --testPathPattern="capa-service" 2>&1 | grep -E "(PASS|FAIL)"`

---

## SA-43 — `src/__tests__/calibracao/calibracao.test.ts`

Testes unitários para calibracao. ~70 LOC.

**Testa:**
- `calculateCalibracaoStatus`: 5 cenários (overdue, warning-7d, warning-30d, in-date, out-of-service)
- `CalibracaoRecord` tipo: campos obrigatórios
- `useCalibracaoTracking` mock: overdueCount calculado corretamente
- `alertsSent` append: não duplicar mesma data

**Verify:** `npm test -- --testPathPattern="calibracao" 2>&1 | grep -E "(PASS|FAIL)"`

---

## SA-44 — `src/__tests__/management-review/management-review.test.ts`

Testes unitários para management review. ~80 LOC.

**Testa:**
- `MANAGEMENT_REVIEW_ENTRY_TITLES.length === 15`
- `ManagementReviewMeeting` com menos de 15 entradas: TS type error (se typed como tuple)
- `aggregateManagementReviewData` mock: retorna sempre 15 entradas mesmo com query errors
- `ReviewSignature` hash: validação 64 chars hex

**Verify:** `npm test -- --testPathPattern="management-review" 2>&1 | grep -E "(PASS|FAIL)"`
