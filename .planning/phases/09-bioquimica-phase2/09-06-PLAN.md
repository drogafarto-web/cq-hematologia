---
phase: 09-bioquimica-phase2
plan: '06'
wave: 5
label: Tests
model: haiku
type: execute
depends_on: ['09-05']
parallel: true
autonomous: true
human_gates: 0
subagents: 5
---

# Phase 9 — Wave 5: Tests (5 subagentes paralelos)

Todos os 5 SAs disparam simultaneamente após W4. Cada SA escreve exatamente um arquivo de testes.

---

## SA-17 — `src/features/bioquimica/__tests__/westgardRulesCLSI.test.ts`

12+ casos cobrindo as 8 regras Westgard + edge cases. Usa `evaluateRules` do SA-05.

**Casos obrigatórios:**

| Suite          | Caso                     | Input                           | Esperado                          |
| -------------- | ------------------------ | ------------------------------- | --------------------------------- |
| 1-2s           | ponto > 2σ               | `[0, 0, 0, 2.5]`, sigma=1       | violation `1-2s`, severity warn   |
| 1-2s           | dentro de 2σ             | `[0.5, 1.0, 1.8]`, sigma=1      | null                              |
| 1-3s           | ponto > 3σ               | `[0, 0, 0, 4]`, sigma=1         | violation `1-3s`, severity reject |
| 2-2s           | 2 consec > 2σ mesmo lado | `[0, 0, 2.5, 2.5]`, sigma=1     | violation `2-2s`                  |
| 2-2s           | lados opostos            | `[0, 0, 2.5, -2.5]`, sigma=1    | sem 2-2s                          |
| R-4s           | range > 4σ               | `[0, 5]`, sigma=1               | violation `R-4s`                  |
| 4-1s (enabled) | 4 consec > 1σ mesmo lado | `[1.5, 1.2, 1.8, 1.4]`, sigma=1 | violation `4-1s`                  |
| 10x (enabled)  | 10 consec mesmo lado     | `Array(10).fill(0.5)`, sigma=1  | violation `10x`                   |
| 6T (enabled)   | 6 crescentes             | `[1,2,3,4,5,6,7]`, sigma=1      | violation `6T`                    |
| edge           | config.enabled = false   | qualquer pontos                 | null                              |
| edge           | points = []              | stdConfig                       | null                              |
| edge           | sem violações            | `[0.5, 0.2, 0.8, 0.1]`, sigma=1 | null                              |

**Padrão de import:**

```typescript
import { evaluateRules } from '../utils/westgardRulesCLSI';
import type { WestgardConfig } from '../types/westgard';
```

**Min lines:** 200  
**Commit:** `test(bioquimica): add 12+ Westgard CLSI tests (Phase 9 W5)`  
**Verify:** `npm test -- src/features/bioquimica/__tests__/westgardRulesCLSI.test.ts`

---

## SA-18 — `functions/src/modules/bioquimica/__tests__/westgardEngine.test.ts`

8+ casos server-side: compliance por analito, bulaPendente, regras extended.

**Casos obrigatórios:**

| Caso | Cenário                              | Esperado                                                    |
| ---- | ------------------------------------ | ----------------------------------------------------------- |
| 1    | violação 1-3s                        | `RunComplianceResult.accepted === false`                    |
| 2    | sem violações                        | `accepted === true`, `violations: []`                       |
| 3    | `bulaPendente: true`                 | `accepted === true`, violação com ruleName `"bulaPendente"` |
| 4    | regra 4-1s habilitada                | detecta 4 consecutivos > 1σ                                 |
| 5    | analitoId não encontrado             | `accepted: false`, violation `"metadata"`                   |
| 6    | `complianceScore` calculado          | `0 ≤ score ≤ 1`                                             |
| 7    | `applicableRules` reflete config     | só regras enabled aparecem                                  |
| 8    | `evaluateRulesServer` espelha client | mesma lógica para inputs idênticos                          |

**Padrão:** mock `Map<string, Analito>` — não acessa Firestore.

**Min lines:** 180  
**Commit:** `test(bioquimica): add 8+ server Westgard engine tests (Phase 9 W5)`  
**Verify:** `cd functions && npm test -- --testPathPattern westgardEngine`

---

## SA-19 — `src/features/bioquimica/__tests__/ocrFuzzyMatch.test.ts`

7+ casos cobrindo todos os ramos do algoritmo de matching.

**Casos obrigatórios:**

| Caso | Input parsedName        | Esperado                                                          |
| ---- | ----------------------- | ----------------------------------------------------------------- |
| 1    | `"Glicose"` (exato)     | confidence 1.0, reason `"exact"`, `requiresOperatorReview: false` |
| 2    | `"GLICOSE"` (case)      | confidence 1.0, reason `"exact"`                                  |
| 3    | `"Glucosa"` (typo)      | matchedAnalitoId glucose, confidence > 0.85                       |
| 4    | `"Albumin"` (semântico) | matchedAnalitoId albumin, reason `"semantic"`                     |
| 5    | `"Gle"` (ambíguo curto) | se match: `requiresOperatorReview: true`                          |
| 6    | `"UnknownAnalyte99"`    | `matchedAnalitoId: undefined`, reason `"no-match"`                |
| 7    | seed vazio `[]`         | `matchedAnalitoId: undefined`                                     |

**Mock seed:** pelo menos `Glicose`, `Albumina`, `Ureia`.

**Min lines:** 120  
**Commit:** `test(bioquimica): add 7+ fuzzy match tests (Phase 9 W5)`  
**Verify:** `npm test -- src/features/bioquimica/__tests__/ocrFuzzyMatch.test.ts`

---

## SA-20 — `src/features/bioquimica/__tests__/ocrIntegration.test.ts`

5 cenários E2E do pipeline OCR (sem chamada real ao Gemini — mock do service).

**Cenários obrigatórios:**

| Cenário | Descrição                                       | Verificação                                           |
| ------- | ----------------------------------------------- | ----------------------------------------------------- |
| 1       | Imagem perfeita (todos os analitos encontrados) | todos matches sem `requiresOperatorReview`            |
| 2       | Baixa confiança (OCR parcial)                   | pelo menos 1 match com `requiresOperatorReview: true` |
| 3       | Analito não reconhecido                         | `unrecognizedAnalytes.length > 0`                     |
| 4       | Match semântico (Albumin → Albumina)            | `matchReason === "semantic"`                          |
| 5       | Override pelo operador                          | `matchedAnalitoId` pode ser substituído manualmente   |

**Padrão:** usar `fuzzyMatchAnalyte` diretamente com mock de `ParsedAnalyte[]` — não montar hooks.

**Min lines:** 120  
**Commit:** `test(bioquimica): add 5 OCR integration scenarios (Phase 9 W5)`  
**Verify:** `npm test -- src/features/bioquimica/__tests__/ocrIntegration.test.ts`

---

## SA-21 — `functions/src/modules/bioquimica/__tests__/geminiOCRParser.test.ts`

3+ casos para o server-side parser: mock global.fetch para simular Gemini.

**Casos obrigatórios:**

| Caso | Cenário                        | Esperado                                       |
| ---- | ------------------------------ | ---------------------------------------------- |
| 1    | Gemini retorna JSON válido     | `analytes.length > 0`, `overallConfidence > 0` |
| 2    | Gemini retorna erro HTTP (401) | `analytes: []`, `errors` preenchido            |
| 3    | Gemini retorna JSON malformado | `analytes: []`, `errors` preenchido            |

**Padrão de mock:**

```typescript
global.fetch = jest.fn(() =>
  Promise.resolve({
    ok: true,
    json: () =>
      Promise.resolve({
        candidates: [{ content: { parts: [{ text: JSON.stringify(mockResult) }] } }],
      }),
  }),
) as jest.Mock;
```

**Min lines:** 80  
**Commit:** `test(bioquimica): add 3+ server OCR parser tests (Phase 9 W5)`  
**Verify:** `cd functions && npm test -- --testPathPattern geminiOCRParser`
