---
phase: 09-bioquimica-phase2
plan: '07'
wave: 6
label: Verification Gate
model: haiku
type: execute
depends_on: ['09-06']
parallel: false
autonomous: true
human_gates: 0
subagents: 1
---

# Phase 9 — Wave 6: Verification Gate (1 subagente)

SA-22 executa os checks em sequência. Se algum falhar, reporta o erro exato e para — **não tenta corrigir**.

---

## SA-22 — Gate Final de Verificação

**Executar em ordem:**

### Check 1 — TypeScript (client)

```bash
npx tsc --noEmit
```

Critério: **0 erros**. Se houver erros, listar os primeiros 10 e parar.

### Check 2 — TypeScript (functions)

```bash
cd functions && npx tsc --noEmit
```

Critério: **0 erros**. Se houver erros, listar os primeiros 10 e parar.

### Check 3 — Testes bioquímica (client)

```bash
npm test -- --passWithNoTests --coverage src/features/bioquimica
```

Critério: **≥ 62 testes passando** (42 regressão Phase 1 + 20 novos Phase 9).  
Se resultado < 62: listar suites com falha.

### Check 4 — Testes bioquímica (functions)

```bash
cd functions && npm test -- --passWithNoTests --testPathPattern bioquimica
```

Critério: **≥ 11 testes passando** (8 westgardEngine + 3 geminiOCRParser).

### Check 5 — Seed count

```bash
grep -c "westgardRules" src/features/bioquimica/constants/seedAnalitos.ts
```

Critério: **≥ 25**. Confirma que os 25+ analitos foram adicionados com westgardRules.

### Check 6 — Audit trail no Firestore path

```bash
grep -r "bioquimica/audit" src/features/bioquimica/services/
```

Critério: **pelo menos 1 match**. Confirma que `ocrValidationService` grava no path correto.

### Check 7 — Compliance keywords

```bash
grep -c "RDC 978\|DICQ 4.3\|Art. 161\|Art. 179" src/features/bioquimica/services/ocrValidationService.ts docs/BIOQUIMICA_OCR_INTEGRATION.md
```

Critério: **≥ 4 ocorrências** combinadas. Confirma presença de referências normativas.

---

## Output do Gate

```
## Phase 9 — Verification Gate Results

| Check | Status | Detail |
|-------|--------|--------|
| TSC client | ✅ / ❌ | 0 erros / N erros |
| TSC functions | ✅ / ❌ | 0 erros / N erros |
| Tests client | ✅ / ❌ | N/62 passing |
| Tests functions | ✅ / ❌ | N/11 passing |
| Seed count | ✅ / ❌ | N analitos |
| Audit path | ✅ / ❌ | match found |
| Compliance refs | ✅ / ❌ | N refs |

### Verdict: PASS / FAIL

[Se PASS]: Phase 9 Wave 0–6 complete. Pronto para deploy.
[Se FAIL]: N checks falharam. Não fazer deploy. Corrigir e re-executar gate.
```

---

## Commit após gate PASS

```
chore(bioquimica): Phase 9 verification gate PASS — 62+ tests, 0 TSC errors, 25+ analitos
```

Se gate FAIL: **não commitar**. Reportar checks falhando ao orquestrador.
