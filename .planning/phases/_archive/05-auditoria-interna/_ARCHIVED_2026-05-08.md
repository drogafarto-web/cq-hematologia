# ARCHIVED 2026-05-08

**Status:** Superseded — module already shipped; v1.4 work moved.

**Canonical replacement:**
- Module already in production — see root `CLAUDE.md` row `auditoria` (Em prod 2026-05-03).
- v1.4 internal-audit-related work tracked in `.planning/phases/13-dicq-audit/`.

## Why archived

This directory was the **v1.2 Phase 5** plan for building the Auditoria Interna module (DICQ 1.3). The module shipped to production on 2026-05-03 (commit `cc03a95` — "fix(auditoria): apply 7 critical/high-priority fixes for Wave 2 deployment"). The phase number `05-` is now reused by v1.4 for Critical Values + IA Strip, which created the directory collision flagged by the audit.

The historical plans (`05-01-PLAN.md` … `05-04-PLAN.md`) and summaries are kept here verbatim for traceability of the v1.2 → v1.3 → v1.4 evolution.

## Audit reference

`.planning/PHASE_5_CRITICAL_FINDINGS.md` (2026-05-08), line 105 — auditor flagged the three competing `05-*` trees as an information-architecture problem.

## Cross-references

- `.planning/phases/07-dry-run/07-01-PLAN.md` (lines 74–75) imports `PHASE_OVERVIEW.md` and `05-01-SUMMARY.md` from this archive. The dry-run plan itself is also a v1.2 artifact and is not in scope for v1.4 execution; if revived for a future audit dry-run, update those `@` references to read from this archive path.
- `.planning/phases/01-v13-stabilization-PLAN.md` line 159 references this directory historically; left as-is since that doc is itself a stabilization record.

## Do not

- Do not edit files here. Module changes go through the live `src/features/auditoria/` codebase and current phase plans.
- Do not delete — kept for traceability.
