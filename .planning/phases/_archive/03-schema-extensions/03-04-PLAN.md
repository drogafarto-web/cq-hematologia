---
phase: "3.4"
title: "Cloud Functions Base Structure + Test Infrastructure"
task: "03-04"
req: "REQ-3.4, REQ-3.5"
owner: "Stream A — DevOps (CTO)"
duration: "2.5 days"
wave: 2
status: "ready"
dependencies:
  - "03-01"
  - "03-03"
---

# Task 03-04 — Cloud Functions Base Structure + Test Infrastructure

**Objective:** Create Cloud Functions v1.4 module skeletons, import shared helpers, set up test infrastructure for portal, NOTIVISA, and IA callables.

**Owner:** Stream A (DevOps, CTO)  
**Duration:** 2.5 days  
**Wave:** 2 (depends on Task 03-01 [schema] + Task 03-03 [helpers])

---

## Module Skeletons to Create

Create placeholder modules for Phases 4–12. Each module will be filled in by later phases.

### 1. `functions/src/modules/notivisa/`

**Purpose:** NOTIVISA notification queue processor (Phase 4).

**Files:**
```
functions/src/modules/notivisa/
├── index.ts (exports: notivisaQueueProcessor callable)
├── types.ts (NotivisaEvent, NotivisaConfig)
└── __tests__/
    └── notivisa.test.ts (basic skeleton tests)
```

**`index.ts` skeleton:**
```typescript
import * as functions from 'firebase-functions/v2/https';
import { notivisaFormatter } from '../../shared/notivisa';

export const notivisaQueueProcessor = functions.onCall(async (request) => {
  // Phase 4: Implement full NOTIVISA queueing + retry logic
  // For now: return placeholder
  return {
    status: 'PLACEHOLDER',
    message: 'NOTIVISA queue processor (Phase 4)'
  };
});
```

### 2. `functions/src/modules/portals/`

**Purpose:** Portal access + patient laudo download (Phase 5–6).

**Files:**
```
functions/src/modules/portals/
├── index.ts (exports: getPortalConfig, downloadLaudoPDF callables)
├── types.ts (PortalConfig, PatientLaudo)
└── __tests__/
    └── portals.test.ts
```

**`index.ts` skeleton:**
```typescript
import * as functions from 'firebase-functions/v2/https';

export const getPortalConfig = functions.onCall(async (request) => {
  // Phase 5: Fetch portal-configuracao for patient UI branding
  return {
    status: 'PLACEHOLDER',
    message: 'Portal config (Phase 5)'
  };
});

export const downloadLaudoPDF = functions.onCall(async (request) => {
  // Phase 6: Generate + download laudo as PDF
  return {
    status: 'PLACEHOLDER',
    message: 'Laudo PDF export (Phase 6)'
  };
});
```

### 3. `functions/src/modules/criticos/`

**Purpose:** Critical value escalation + SMS routing (Phase 7).

**Files:**
```
functions/src/modules/criticos/
├── index.ts (exports: escalateCriticalValue callable)
├── types.ts (Critico, EscalationConfig)
└── __tests__/
    └── criticos.test.ts
```

**`index.ts` skeleton:**
```typescript
import * as functions from 'firebase-functions/v2/https';
import { smsTemplate } from '../../shared/sms';

export const escalateCriticalValue = functions.onCall(async (request) => {
  // Phase 7: Implement SMS + email escalation for critical values
  // Use smsTemplate helper
  return {
    status: 'PLACEHOLDER',
    message: 'Critical value escalation (Phase 7)'
  };
});
```

### 4. `functions/src/modules/ia-strip/`

**Purpose:** IA strip image upload + OCR classification (Phase 9).

**Files:**
```
functions/src/modules/ia-strip/
├── index.ts (exports: uploadStripImage, classifyStrip callables)
├── types.ts (StripImage, ClassificationResult)
└── __tests__/
    └── ia-strip.test.ts
```

**`index.ts` skeleton:**
```typescript
import * as functions from 'firebase-functions/v2/https';
import { validateStripImage } from '../../shared/ia';

export const uploadStripImage = functions.onCall(async (request) => {
  // Phase 9: Validate + upload strip image to Cloud Storage
  const image = validateStripImage(request.data); // Use shared validator
  return {
    status: 'PLACEHOLDER',
    imageUrl: 'https://placeholder.com/image.jpg',
    message: 'Strip image upload (Phase 9)'
  };
});

export const classifyStrip = functions.onCall(async (request) => {
  // Phase 9: Call Gemini Vision API for strip classification
  return {
    status: 'PLACEHOLDER',
    classes: ['IgG', 'IgM'],
    confidence: 0.95
  };
});
```

---

## Test Infrastructure

### 1. Test Fixtures

Create reusable test data in `functions/src/__tests__/fixtures/`:

**`notivisa-payloads.ts`:**
```typescript
export const mockNotivisaEvent = {
  laudo_id: 'laudo-test-001',
  patient_cpf: '12345678901',
  payload: { versao: '1.0', ... },
  status: 'PENDING',
  attempts: 0,
  createdAt: new Date()
};
```

**`portal-users.ts`:**
```typescript
export const mockPatient = {
  uid: 'patient-123',
  email: 'patient@example.com',
  role: 'PATIENT',
  labId: 'test-lab-001'
};
```

**`critico-thresholds.ts`:**
```typescript
export const mockCriticoConfig = {
  analito: 'potassium',
  limite_inferior: 3.5,
  limite_superior: 5.5,
  sms_escalation: true,
  email_escalation: true
};
```

### 2. Integration Test Suite

**`functions/src/__tests__/integration/notivisa.test.ts`:**
```typescript
describe('NOTIVISA Module', () => {
  test('notivisaQueueProcessor returns placeholder', async () => {
    const result = await notivisaQueueProcessor({ data: {} });
    expect(result.status).toBe('PLACEHOLDER');
  });
});
```

**`functions/src/__tests__/integration/portals.test.ts`:**
```typescript
describe('Portal Module', () => {
  test('getPortalConfig returns placeholder', async () => {
    const result = await getPortalConfig({ data: { labId: 'test-lab' } });
    expect(result.status).toBe('PLACEHOLDER');
  });
});
```

**`functions/src/__tests__/integration/criticos.test.ts`:**
```typescript
describe('Críticos Module', () => {
  test('escalateCriticalValue imports smsTemplate', () => {
    // Verify smsTemplate is callable
    const sms = smsTemplate(mockCriticoConfig, mockLab, mockPaciente);
    expect(sms.length).toBeLessThanOrEqual(160);
  });
});
```

---

## File Structure

```
functions/
├── src/
│   ├── modules/
│   │   ├── notivisa/
│   │   │   ├── index.ts
│   │   │   ├── types.ts
│   │   │   └── __tests__/
│   │   │       └── notivisa.test.ts
│   │   ├── portals/
│   │   │   ├── index.ts
│   │   │   ├── types.ts
│   │   │   └── __tests__/
│   │   │       └── portals.test.ts
│   │   ├── criticos/
│   │   │   ├── index.ts
│   │   │   ├── types.ts
│   │   │   └── __tests__/
│   │   │       └── criticos.test.ts
│   │   └── ia-strip/
│   │       ├── index.ts
│   │       ├── types.ts
│   │       └── __tests__/
│   │           └── ia-strip.test.ts
│   ├── shared/
│   │   ├── notivisa.ts ✅ (created in Task 03-03)
│   │   ├── sms.ts ✅
│   │   ├── laudo.ts ✅
│   │   └── ia.ts ✅
│   └── __tests__/
│       ├── fixtures/
│       │   ├── notivisa-payloads.ts
│       │   ├── portal-users.ts
│       │   └── critico-thresholds.ts
│       └── integration/
│           ├── notivisa.test.ts
│           ├── portals.test.ts
│           └── criticos.test.ts
├── package.json (verify all dependencies present)
└── tsconfig.json (ensure strict mode enabled)
```

---

## Implementation Steps

1. **Create 4 module directories** with `index.ts`, `types.ts`, `__tests__/` subdirs
2. **Write callable skeletons** — all return `{ status: 'PLACEHOLDER' }`
3. **Import shared helpers** — `notivivsa.ts`, `sms.ts`, `ia.ts` at top of each module
4. **Create test fixtures** — reusable mock data for all tests
5. **Write integration tests** — 3 suites, ≥10 tests total
6. **Build + typecheck:**
   ```bash
   cd functions
   npm run typecheck
   npm run test
   npm run build
   ```

---

## Validation Checklist

- [ ] All 4 module directories created
- [ ] All `index.ts` files have callable skeletons
- [ ] All imports from `shared/` are correct
- [ ] 0 TypeScript errors (`npm run typecheck`)
- [ ] All integration tests pass (`npm run test`)
- [ ] Functions build succeeds (`npm run build`)
- [ ] No `console.log` statements in skeletons
- [ ] All files follow linting rules

---

## Testing

**Run locally:**
```bash
cd functions
npm install
npm run typecheck  # 0 errors
npm run test       # All pass
npm run build      # Success
```

**Expected output:**
```
PASS  src/__tests__/integration/notivisa.test.ts
PASS  src/__tests__/integration/portals.test.ts
PASS  src/__tests__/integration/criticos.test.ts
...
Test Suites: 3 passed, 3 total
Tests: 10 passed, 10 total
```

---

## Success Criteria

- ✅ All 4 module skeletons created
- ✅ 0 TypeScript errors
- ✅ All tests passing (≥10 tests)
- ✅ Functions build succeeds
- ✅ Ready for Phase 4+ implementation

---

## Transition to Phase 4+

Each later phase will:
1. Take over the module skeleton (e.g., Phase 4 fills notivisa/)
2. Implement full business logic
3. Add comprehensive tests
4. Deploy to staging → production

Example (Phase 4):
```typescript
// Phase 4: Replace placeholder notivisaQueueProcessor with full implementation
export const notivisaQueueProcessor = functions.onCall(async (request) => {
  const event = await getNotivisaEvent(request.data.eventId);
  const payload = notivisaFormatter(event.laudo, event.paciente);
  // ... retry logic, send to NOTIVISA API, etc.
  return { status: 'SENT', eventId: event.id };
});
```

---

**Created:** 2026-05-07  
**Wave:** 2 (depends on 03-01 + 03-03)  
**Status:** Ready to execute
