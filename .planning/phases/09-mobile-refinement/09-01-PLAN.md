---
phase: 09-mobile-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  [src/screens, src/components, src/theme/darkTheme.ts, src/__tests__/screens, tailwind.config.js]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - 'All 26 mobile screens render with dark background colors only'
    - 'No white text on dark background creates low contrast (WCAG AA ≥4.5:1)'
    - 'Typography hierarchy is applied consistently (font sizes, weights, spacing)'
    - 'Visual regressions detected via snapshot tests'
  artifacts:
    - path: 'src/theme/darkTheme.ts'
      provides: 'Complete dark color token system'
      exports: 'colors, spacing, typography'
    - path: 'src/screens/**/*.tsx'
      provides: '26 screens with dark mode compliance'
      min_lines: 'Updated to use tokens, no hardcoded colors'
    - path: 'src/components/**/*.tsx'
      provides: 'Reusable dark-themed UI components'
      min_lines: 'Updated dark:* variants'
    - path: 'src/__tests__/screens/darkMode.test.ts'
      provides: 'Visual regression snapshots'
      exports: '10+ snapshot tests'
  key_links:
    - from: 'src/screens/**'
      to: 'src/theme/darkTheme.ts'
      via: 'useDarkMode() hook + token imports'
      pattern: 'darkTheme.colors|useDarkMode()'
    - from: 'tailwind.config.js'
      to: 'src/**/*.tsx'
      via: 'dark: variant support'
      pattern: 'dark:'
---

<objective>
Audit all 26 mobile screens for dark mode compliance. Fix light-mode color leakage, apply dark-first design system using tokens and Tailwind dark variants. Achieve 100% dark mode coverage with visual regression protection.

**Purpose:** HC Quality mobile is dark-first per CLAUDE.md. Phase 3.3 scaffold is incomplete — some screens have hardcoded white, low-contrast text, or missing dark: variants. This plan validates every screen and fixes compliance issues.

**Output:**

- Complete darkTheme.ts with all required color tokens
- All 26 screens using theme tokens (no hardcoded RGB/#fff)
- 10+ visual snapshot tests for dark mode regression detection
- Zero low-contrast issues (WCAG AA baseline)
  </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\hc quality\hc-quality-mobile\package.json
@C:\hc quality\hc-quality-mobile\src\theme\darkTheme.ts
@C:\hc quality\hc-quality-mobile\tailwind.config.js
@C:\hc quality\CLAUDE.md (global design standards)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Screen Audit + Dark Mode Gap Analysis</name>
  <files>
    .planning/phases/09-mobile-refinement/09-01-DARK-MODE-AUDIT.md (output)
  </files>
  <action>
**Do not implement fixes yet — only audit and document gaps.**

1. Visual screenshot audit of all 26 screens in dark mode:
   - Navigate emulator to each screen (use React Native Debugger or Expo CLI to inspect navigation)
   - Take screenshot of each:
     - HomeScreen
     - CIQScreen, CIQDetailScreen
     - NCScreen, NCDetailScreen
     - ReadingsScreen
     - TrainingScreen
     - OfflineQueueScreen
     - AuthScreen
     - Settings/Config screens (if any)
     - Component library screens (if previewed)
   - Inspect colors: are backgrounds dark (≥90% black), are text colors light (white or light gray)?

2. For each screen, document issues:
   - If background is white/light → "Light background on [ScreenName]"
   - If text is dark on light background → "Dark text contrast issue: [component]"
   - If missing `dark:` variant in className → "Tailwind dark: missing: [className]"
   - If hardcoded RGB/hex color → "Hardcoded color: [file:line] = [color value]"
   - If spacing doesn't follow 4px grid → "Non-grid spacing: [value]px"

3. Generate audit report (markdown table):

   ```
   | Screen | Component | Issue | Priority | Fix |
   |--------|-----------|-------|----------|-----|
   | HomeScreen | Card | White background | P0 | Apply dark:bg-black |
   | CIQScreen | Button | #ffffff hardcoded | P0 | Use darkTheme.colors.text |
   ```

4. Extract incomplete tokens from darkTheme.ts:
   - List colors NOT yet defined (e.g., "secondaryBackground", "errorRed")
   - List unused tokens (defined but no references)
   - Check Tailwind config: do all colors used in screens have dark: equivalents?

5. Run Tailwind lint (to catch config errors):

   ```bash
   cd hc-quality-mobile
   npx tailwindcss lint
   ```

6. Commit audit report: `docs(mobile): dark mode audit — gaps identified`
   </action>
   <verify>
   Audit report exists at `.planning/phases/09-mobile-refinement/09-01-DARK-MODE-AUDIT.md` with:

- Screenshot list (all 26 screens documented)
- Gap table (≥5 issues identified, ≤30 total issues — else screens broken)
- Token list (new tokens needed for fixes)
- Tailwind lint output (green, no errors)
  </verify>
  <done>Audit complete. All screens visually inspected. Gaps documented with priority and fix strategy.</done>
  </task>

<task type="auto">
  <name>Task 2: Dark Mode Theme Completion + Component Token Refactor</name>
  <files>
    src/theme/darkTheme.ts,
    src/screens/HomeScreen.tsx,
    src/screens/CIQScreen.tsx,
    src/screens/CIQDetailScreen.tsx,
    src/screens/NCScreen.tsx,
    src/screens/NCDetailScreen.tsx,
    src/screens/ReadingsScreen.tsx,
    src/screens/TrainingScreen.tsx,
    src/screens/AuthScreen.tsx,
    src/screens/OfflineQueueScreen.tsx,
    src/components/CIQCard.tsx,
    src/components/NCCard.tsx,
    src/components/ReadingForm.tsx,
    src/components/SignatureModal.tsx,
    src/components/SyncStatusBanner.tsx,
    src/components/OfflineIndicator.tsx,
    src/hooks/useDarkMode.ts
  </files>
  <action>
**Phase 1 (darkTheme.ts completion):**

1. Open `src/theme/darkTheme.ts`. Compare against Phase 3.3 baseline and audit gaps (from Task 1).

2. Ensure all required tokens exist:

   ```typescript
   export const darkTheme = {
     colors: {
       // Backgrounds
       bg: '#141417', // Main dark background (near black)
       bgSecondary: '#1f1f23', // Cards, sections (lighter)
       bgTertiary: '#2a2a2f', // Hover, active states

       // Text
       text: '#f5f5f7', // Primary text (light gray)
       textSecondary: '#a1a1a6', // Secondary text, labels
       textTertiary: '#86868b', // Tertiary, hints

       // Semantic
       success: '#30B0C0', // Emerald green (CIQ good)
       error: '#FF3B30', // Red (errors, NCs)
       warning: '#FF9500', // Orange (warnings)
       info: '#5AC8FA', // Blue (info)

       // Component-specific
       cardBg: '#1f1f23',
       border: '#424245', // Divider, subtle borders
       borderLight: '#5a5a5f', // Hover borders
     },
     spacing: {
       // 4px grid
       1: '4px',
       2: '8px',
       3: '12px',
       4: '16px',
       6: '24px',
       8: '32px',
     },
     typography: {
       heading: { fontSize: 32, fontWeight: '700', lineHeight: 1.2 },
       body: { fontSize: 16, fontWeight: '400', lineHeight: 1.5 },
       caption: { fontSize: 12, fontWeight: '400', lineHeight: 1.4 },
     },
   };
   ```

3. Add missing tokens from audit (Task 1). If audit found "need secondaryBackground", add it now.

4. Commit: `feat(mobile): complete darkTheme token system`

**Phase 2 (Refactor components to use tokens):**

5. For each screen file in `src/screens/`:
   - Remove hardcoded colors: grep for `'#ffffff'`, `'#000000'`, `'rgb('`, `'hsl('`
   - Replace with token references:
     - `backgroundColor: '#ffffff'` → `backgroundColor: darkTheme.colors.text` (or create a semantically correct token)
     - Hardcoded dark gray → use `darkTheme.colors.bgSecondary`
   - Update classNames to use `dark:` variants where applicable:
     - `className="bg-white dark:bg-black"` → just `className="dark:bg-black"` (if dark-first, no light variant needed)
     - Verify Tailwind config has dark: support enabled

6. For each component file in `src/components/`:
   - Same refactor (remove hardcoded, use tokens)
   - CIQCard, NCCard, ReadingForm, SignatureModal, etc. should all read from darkTheme

7. Update `src/hooks/useDarkMode.ts`:
   - Ensure hook returns `theme: darkTheme` (it should, based on Phase 3.3 baseline)
   - Verify hook is used by screens/components (they import and call it if needed)

8. Grep check (to verify no hardcoded colors remain):

   ```bash
   cd hc-quality-mobile
   grep -r "'#[0-9a-f]\{3,6\}'" src/screens src/components --include="*.tsx" | wc -l
   # Should return 0 (or only in theme files)
   grep -r '"#[0-9a-f]\{3,6\}"' src/screens src/components --include="*.tsx" | wc -l
   # Should return 0
   grep -r "rgb(" src/screens src/components --include="*.tsx" | wc -l
   # Should return 0
   ```

9. Linter check:

   ```bash
   npm run lint
   ```

   Should pass with no color-related warnings.

10. Commit: `feat(mobile): all screens + components use darkTheme tokens`
    </action>
    <verify>

- darkTheme.ts contains all required color, spacing, typography tokens (manual inspection)
- Grep returns 0 for hardcoded colors in src/screens and src/components (except in comments)
- `npm run lint` passes green
- `npm run build` succeeds (no TS errors related to theme)
  </verify>
  <done>All screens and components refactored to use darkTheme tokens. No hardcoded colors. Lint clean.</done>
  </task>

<task type="auto">
  <name>Task 3: Visual Regression Tests + Dark Mode Coverage</name>
  <files>
    src/__tests__/screens/darkMode.test.ts,
    src/__tests__/components/darkMode.test.ts
  </files>
  <action>
1. Create Jest test file for screen dark mode snapshots:

`src/__tests__/screens/darkMode.test.ts`:

```typescript
import React from 'react';
import { render } from '@testing-library/react-native';
import { DarkModeProvider } from '../../context/DarkModeContext'; // or your dark mode setup

import HomeScreen from '../../screens/HomeScreen';
import CIQScreen from '../../screens/CIQScreen';
import NCScreen from '../../screens/NCScreen';
import ReadingsScreen from '../../screens/ReadingsScreen';
import AuthScreen from '../../screens/AuthScreen';
// ... import other key screens

describe('Dark Mode Coverage — Screen Snapshots', () => {
  it('HomeScreen renders with dark theme', () => {
    const { toJSON } = render(
      <DarkModeProvider>
        <HomeScreen />
      </DarkModeProvider>
    );
    expect(toJSON()).toMatchSnapshot();
  });

  it('CIQScreen renders with dark theme', () => {
    // Same pattern
  });

  // ... repeat for 8–10 key screens
});
```

2. Establish baseline:

   ```bash
   cd hc-quality-mobile
   npm run test:unit -- --testPathPattern=darkMode --updateSnapshot
   ```

   This creates snapshot files (`.snap` files).

3. Verify snapshots capture dark theme:
   - Open generated `.snap` file
   - Grep for color tokens: `#141417`, `#1f1f23`, `#f5f5f7` should appear
   - Grep should NOT find `#ffffff`, `#000000` (except in comments)

4. Add color contrast test helper:

   ```typescript
   // Helper to check WCAG AA contrast
   function checkContrast(bgColor: string, textColor: string): boolean {
     // Simple check: light text on dark bg passes
     // Can use npm package `polished` or implement basic luminance check
     const isDarkBg = bgColor.includes('141417') || bgColor.includes('1f1f23');
     const isLightText = textColor.includes('f5f5f7') || textColor.includes('a1a1a6');
     return isDarkBg && isLightText;
   }
   ```

5. Create component snapshot tests similarly (`src/__tests__/components/darkMode.test.ts`).

6. Run full test suite:

   ```bash
   npm run test:unit -- --testPathPattern=darkMode
   ```

   Should pass with all snapshots created.

7. Commit: `test(mobile): dark mode visual regression snapshots`
   </action>
   <verify>

- Snapshot files exist (`*.snap`)
- Snapshot files contain dark theme colors (#141417, #1f1f23, etc.)
- No snapshots contain hardcoded light colors
- `npm run test:unit -- --testPathPattern=darkMode` passes
- Contrast check helper passes (light text on dark background)
  </verify>
  <done>10+ dark mode snapshots created. Visual regression tests automated. Baseline established.</done>
  </task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary             | Description                                                       |
| -------------------- | ----------------------------------------------------------------- |
| Device display input | User's system dark mode preference (read-only via useColorScheme) |
| Theme token export   | Ensure darkTheme.ts exports are immutable (const, not let)        |

## STRIDE Threat Register

| Threat ID | Category               | Component                          | Disposition | Mitigation Plan                                                          |
| --------- | ---------------------- | ---------------------------------- | ----------- | ------------------------------------------------------------------------ |
| T-09-01   | Information Disclosure | darkTheme.ts (contains no secrets) | accept      | No PII, hardcoded design tokens only                                     |
| T-09-02   | Denial of Service      | Invalid theme token in rendering   | mitigate    | TypeScript types enforce valid colors; snapshot tests validate rendering |

</threat_model>

<verification>

**Phase 09-01 Completion Checklist:**

- [ ] Audit report exists with all 26 screens documented
- [ ] darkTheme.ts contains all required tokens (colors, spacing, typography)
- [ ] All screens/components refactored: grep shows 0 hardcoded colors
- [ ] `npm run lint` passes
- [ ] `npm run test:unit -- --testPathPattern=darkMode` passes
- [ ] 10+ visual regression snapshots committed
- [ ] Contrast check: light text on dark background verified

</verification>

<success_criteria>

| Criterion                  | Measurement               | Target              |
| -------------------------- | ------------------------- | ------------------- |
| Dark mode audit completion | 26 screens documented     | 100%                |
| Hardcoded color removal    | grep result               | 0 matches           |
| Token coverage             | darkTheme.ts completeness | All required tokens |
| Lint status                | `npm run lint` exit code  | 0 (pass)            |
| Visual regression          | Snapshot test pass rate   | 100%                |
| WCAG AA contrast           | Light text on dark bg     | Pass                |

</success_criteria>

<output>
After completion, create `.planning/phases/09-mobile-refinement/09-01-SUMMARY.md` with:
- Audit results (all 26 screens, gaps resolved)
- Token system documentation
- Test coverage metrics
- Before/after color usage comparison
</output>
