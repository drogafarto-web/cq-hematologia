---
phase: 07-advanced-auditoria
plan: '04'
wave: 3
label: Functions and Hooks
model: haiku
type: execute
depends_on: ['07-03']
parallel: true
autonomous: true
human_gates: 0
subagents: 5
---

# Phase 7 — Wave 3: Functions + Hooks (5 subagentes paralelos)

---

## SA-11 — `functions/src/modules/qualidade/cfAuditTrigger.ts`

```typescript
import { onDocumentCreated } from 'firebase-functions/v2/firestore';
import { detectAnomalies } from './anomalyDetector';
import { generateAlert } from './alertEngine';

export const onAuditTrailEntry = onDocumentCreated(
  {
    document: 'labs/{labId}/audit-trail/{entryId}',
    region: 'southamerica-east1',
    memory: '256MiB',
    timeoutSeconds: 30,
  },
  async (event) => {
    // 1. Extrair dados do event (labId do path, entry do data())
    // 2. detectAnomalies(db, labId, entry) — try/catch silencioso
    // 3. Salvar score em labs/{labId}/anomaly-scores sempre
    // 4. Se overall >= 0.85 → generateAlert(db, labId, score)
    // Falha silenciosa com log — não pode bloquear o audit trail
  },
);
```

≤80 LOC. **Done:** export `onAuditTrailEntry` + região correta + `npx tsc --noEmit` limpo.

---

## SA-12 — `src/features/qualidade/hooks/useAnomalyAlerts.ts`

```typescript
export interface UseAnomalyAlertsReturn {
  alerts: AuditAlert[];
  loading: boolean;
  error: string | null;
  dismissAlert: (alertId: string, reason: string) => Promise<void>;
}

export function useAnomalyAlerts(labId: string): UseAnomalyAlertsReturn;
// onSnapshot em labs/{labId}/audit-alerts onde status=='active', orderBy createdAt desc, limit 50
// dismissAlert → callable 'dismissAuditAlert' com { labId, alertId, reason }
// Cleanup unsubscribe no unmount
```

Seguir padrão de qualquer hook onSnapshot existente no projeto. ≤100 LOC.

**Done:** export + cleanup correto + `npx tsc --noEmit` limpo.

---

## SA-13 — `functions/src/shared/nlpSummarizer.ts` + `__tests__/nlpSummarizer.test.ts`

```typescript
export async function summarizeAuditFindings(params: {
  entryCount: number;
  anomalyCount: number;
  period: string;
  topModules: string[];
  criticalCount: number;
  highCount: number;
}): Promise<string>; // 150-200 palavras, português executivo
// Modelo: gemini-2.5-flash-latest, timeout 8s
// Falha → retornar: `"Relatório de auditoria gerado. ${params.entryCount} operações analisadas em ${params.period}."`
```

≤80 LOC. **Testes (3 casos, Gemini mockado):** resposta válida, timeout→fallback, erro→fallback.

**Done:** export + `npm test -- nlpSummarizer` 100%.

---

## SA-14 — `functions/src/modules/qualidade/reportGenerator.ts`

```typescript
import { onCall } from 'firebase-functions/v2/https';

export const generateAuditReport = onCall<ReportFilter, Promise<AuditReport>>(
  { region: 'southamerica-east1', memory: '512MiB', timeoutSeconds: 60 },
  async (request) => {
    // 1. Validar request.auth + request.data.labId
    // 2. Query labs/{labId}/audit-trail com filtros, limit 5000
    // 3. Agregar métricas (entryCount, anomalyCount, topModules)
    // 4. summarizeAuditFindings(métricas)
    // 5. Salvar em labs/{labId}/audit-reports/{auto-id}
    // 6. Retornar AuditReport
  },
);
```

≤150 LOC. **Done:** export + região correta + `npx tsc --noEmit` limpo.

---

## SA-15 — `src/features/qualidade/hooks/useAuditReportExport.ts`

```typescript
export interface UseAuditReportExportReturn {
  generating: boolean;
  report: AuditReport | null;
  error: string | null;
  generate: (filter: ReportFilter) => Promise<void>;
  reset: () => void;
}

export function useAuditReportExport(): UseAuditReportExportReturn;
// generate → httpsCallable('generateAuditReport')(filter)
// generating:true durante call; report preenchido em sucesso; error em falha
```

≤80 LOC. **Done:** export + `npx tsc --noEmit` limpo.

---

## Gate W3

```bash
npx tsc --noEmit && npm test -- --testPathPattern="nlpSummarizer" --passWithNoTests
```
