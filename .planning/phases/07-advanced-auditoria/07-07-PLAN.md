---
phase: 07-advanced-auditoria
plan: "07"
wave: 6
label: Verification Gate
model: haiku
type: execute
depends_on: ["07-06"]
parallel: false
autonomous: true
human_gates: 0
subagents: 1
---

# Phase 7 — Wave 6: Verification Gate (1 subagente)

**Objetivo:** Gate final autônomo. Roda todos os checks em sequência. Falha → loga o problema e para. Sucesso → escreve SUMMARY.

---

## SA-22 — Gate de verificação final

**Modelo:** Haiku  
**Dependências:** todos os artefatos de W0–W5  
**Contrato:** executar os 6 checks abaixo em ordem. Se qualquer um falhar, parar e logar qual falhou.

---

### Check 1 — TypeScript clean

```bash
cd "C:\hc quality"
npx tsc --noEmit 2>&1
```

**Critério:** zero linhas de erro. Qualquer linha contendo `error TS` = falha.

---

### Check 2 — Testes unitários passando

```bash
npm test -- \
  --testPathPattern="auditDiffDetector|contextCapture|normalizeBaseline|aiPatternMatcher|anomalyDetector|alertEngine|nlpSummarizer|phase7" \
  --passWithNoTests \
  --coverage \
  --coverageThreshold='{"global":{"branches":75}}' \
  2>&1
```

**Critério:** nenhum teste `FAIL`. Coverage branches ≥75%.

---

### Check 3 — Artefatos existem

Verificar que todos os arquivos abaixo existem:

```
src/features/qualidade/types/anomalyTypes.ts
functions/src/shared/auditDiffDetector.ts
functions/src/shared/contextCapture.ts
functions/src/shared/aiPatternMatcher.ts
functions/src/shared/nlpSummarizer.ts
functions/src/modules/qualidade/normalizeBaseline.ts
functions/src/modules/qualidade/anomalyDetector.ts
functions/src/modules/qualidade/alertEngine.ts
functions/src/modules/qualidade/cfAuditTrigger.ts
functions/src/modules/qualidade/reportGenerator.ts
functions/src/modules/qualidade/scheduledReportJob.ts
src/features/qualidade/hooks/useAnomalyAlerts.ts
src/features/qualidade/hooks/useAuditReportExport.ts
src/features/qualidade/components/AlertCenter.tsx
src/features/qualidade/components/AlertDrillDown.tsx
src/features/qualidade/components/ReportBuilder.tsx
src/__tests__/phase7.integration.test.ts
```

**Critério:** todos os 17 arquivos existem.

---

### Check 4 — Firestore rules válidas

```bash
# Verificar que os blocos novos estão presentes
grep -c "audit-trail\|audit-alerts" firestore.rules
```

**Critério:** resultado ≥ 2 (ambos os blocos presentes).

---

### Check 5 — Indexes novos presentes

```bash
grep -c "audit-trail\|audit-alerts\|anomaly-scores" firestore.indexes.json
```

**Critério:** resultado ≥ 5 (pelo menos 5 ocorrências dos novos collection groups).

---

### Check 6 — Compliance mapping

Verificar que os arquivos abaixo contêm referências aos requisitos regulatórios:

```bash
grep -l "RDC 978\|DICQ 4.4" \
  functions/src/modules/qualidade/anomalyDetector.ts \
  functions/src/modules/qualidade/alertEngine.ts \
  functions/src/modules/qualidade/reportGenerator.ts \
  2>/dev/null | wc -l
```

**Critério:** ≥ 2 arquivos contêm referências de compliance (como comentários).

---

### Ação após todos os checks

**Se todos passaram:** criar `07-07-SUMMARY.md` com:

```markdown
# Phase 7 Verification — PASSED

**Data:** {data}
**Checks:** 6/6 ✓

## Artefatos entregues
- 17 arquivos criados/modificados
- 22 subagentes em 7 waves
- 0 intervenção humana

## Métricas
- TSC: 0 erros
- Testes: N passando, N% coverage
- Firestore rules: blocos audit-trail + audit-alerts presentes
- Indexes: 5 novos indexes

## Próximo passo
`/hcq-deploy-gates` → deploy gates antes de abrir PR
```

**Se algum check falhou:** logar qual check falhou e qual foi o output. Não criar SUMMARY. Reportar falha.

---

## Gate final de deploy

Após verificação OK, rodar:

```bash
bash scripts/preflight-secrets-check.sh
```

**Critério:** saída `GREEN` ou `No secrets found`.

Se passou: Phase 7 completa. Aguardar aprovação manual de deploy (único gate humano de toda a phase — só para o push de produção).
