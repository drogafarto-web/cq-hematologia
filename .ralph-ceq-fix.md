Você é um engenheiro senior especializado em Firebase, Firestore Rules, Auth Claims, React, Zustand e sistemas multi-tenant enterprise.

OBJETIVO:
Resolver o problema de permissão do módulo CEQ no HC Quality SEM quebrar outros módulos.

CONTEXTO:
O sistema HC Quality usa:

- Firebase Auth
- Firestore
- Firestore Rules
- custom claims
- multi-tenancy via labs/{labId}
- Zustand
- listeners realtime
- polling fallback
- hasModuleAccess()
- provisionModulesClaims()
- controle de módulos por claims

PROBLEMA OBSERVADO:

- UI mostra: 'Permissão CEQ indisponível'
- Toast mostra: 'CEQ: permissão negada'
- Console apresenta:
  - permission denied
  - listeners falhando
  - ERR_BLOCKED_BY_CLIENT
  - requests firestore Listen/channel falhando
- Sistema entra em degraded mode
- Há possibilidade de:
  - token stale
  - refresh incompleto
  - race condition
  - listener iniciar antes das claims
  - membership inconsistente
  - erro de transporte sendo tratado como permissão

TAREFAS:

ETAPA 1 — MAPEAR FLUXO COMPLETO

1. Analise:

- autenticação
- login
- logout
- refresh de token
- troca de laboratório
- Zustand auth store
- fluxo de custom claims
- provisionModulesClaims
- hasModuleAccess
- listeners CEQ
- degraded transport
- fallback polling

2. Descubra:

- onde modules.ceq deveria existir
- quem cria a claim
- quando ela é atualizada
- quando o frontend lê essa claim
- se há race condition
- se o token não é atualizado após provisionamento
- se há cache stale
- se membership do laboratório diverge

3. Identifique:

- todos os arquivos envolvidos
- todos os listeners envolvidos
- todos os hooks envolvidos
- todas as Firestore Rules relacionadas ao CEQ

IMPORTANTE:
NAO IMPLEMENTAR AINDA.
Primeiro explique profundamente:

- causa raiz provável
- fluxo completo
- dependências
- riscos arquiteturais

ETAPA 2 — INVESTIGAR TRANSPORTE E BLOQUEIOS

1. Verifique:

- listeners Firestore
- websocket
- long polling
- realtime fallback
- tratamento de erro
- degraded mode

2. Descubra:

- se ERR_BLOCKED_BY_CLIENT é causado por:
  - uBlock
  - Brave Shields
  - extensões
  - CSP
  - bloqueio de googleapis

3. Analise:

- se o sistema está confundindo:
  - falha de transporte
  - com permission denied

4. Validar:

- fallback polling
- retry logic
- recovery automático
- mensagens corretas para usuário

ETAPA 3 — IMPLEMENTAR CORREÇÃO MÍNIMA SEGURA
OBJETIVO:
Corrigir SEM quebrar arquitetura.

Implementar:

- refresh seguro de token
- validação robusta de claims
- retry controlado
- separação clara entre:
  - erro de auth
  - erro de transporte
  - erro de listener
- observabilidade
- logs estruturados

Adicionar:

- logs Sentry
- métricas
- tracing de auth
- tracing de listeners

IMPORTANTE:

- não quebrar outros módulos
- não alterar módulos não relacionados
- não introduzir overengineering

ETAPA 4 — TESTES OBRIGATÓRIOS

Executar:

1. Login normal
2. Logout/login
3. Refresh de token
4. Troca de laboratório
5. Usuário com claim
6. Usuário sem claim
7. Usuário sem membership
8. Múltiplas abas
9. Browser com adblock
10. Realtime bloqueado
11. Polling fallback
12. Sessão expirada
13. Claims stale
14. Reconnect de internet
15. Browser refresh

VALIDAR:

- CEQ abre corretamente
- listeners recuperam
- fallback funciona
- UI não confunde erro
- sem loops infinitos
- sem listeners duplicados
- sem memory leaks

ETAPA 5 — RELATÓRIO FINAL

Gerar:

- causa raiz
- arquivos alterados
- riscos restantes
- melhorias futuras
- impacto arquitetural
- testes executados
- rollback seguro
- próximos gargalos possíveis

PENSAR COMO:

- arquiteto enterprise
- especialista Firebase
- especialista Firestore
- especialista sistemas laboratoriais
- especialista multi-tenant

PRIORIDADE ABSOLUTA:

- integridade
- estabilidade
- previsibilidade
- rastreabilidade
- não quebrar outros módulos
