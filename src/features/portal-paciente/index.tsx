/**
 * Portal Paciente
 * Phase 4 — Patient dashboard with LGPD consent flow
 * Accessible, dark-first, world-class medical portal
 */

export { PortalPacienteShell } from './components';
export type { PatientResult, PatientConsent, ConsentScope } from './types';
export { usePatientResults, usePatientConsent } from './hooks';
