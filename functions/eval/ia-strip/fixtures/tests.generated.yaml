# AUTO-GENERATED by scripts/build-eval-fixtures.mjs (TBD)
# DO NOT EDIT BY HAND — regenerate from manifest.json.
#
# This file is a STUB. Until manifest.json is populated with labeled fixtures,
# `npm run eval:ia-strip` will exit 1 with the message below. That is intentional:
# we want CI to fail loudly until real fixtures land, not silently pass on no data.
#
# To populate: see ../README.md and docs/ai-evals/IA_STRIP_EVAL_FRAMEWORK.md.

- description: "STUB — no fixtures provisioned yet"
  vars:
    _stub: true
  assert:
    - type: javascript
      value: |
        throw new Error(
          'IA-Strip eval fixtures not provisioned. ' +
          'See functions/eval/ia-strip/fixtures/README.md to add labeled images, ' +
          'then regenerate tests.generated.yaml from manifest.json.'
        );
