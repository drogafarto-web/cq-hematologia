# Firestore / Firebase emulators (local only — no secrets)
# Carregado por scripts/firestore-emulator-setup.{ps1,sh}
# Sobrescreva localmente se as portas estiverem em uso.
FIREBASE_PROJECT_ID=hmatologia2
FIRESTORE_EMULATOR_PORT=8080
FIREBASE_EMULATOR_UI_PORT=4000
TEST_LAB_IDS=TEST-LAB-001,TEST-LAB-002,TEST-LAB-003
SEED_DATA_DIR=.firebase/emulator-backups
